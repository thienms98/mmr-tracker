# Postgres connection string (Supabase / Neon / Railway free tier đều được)
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# Secret để bảo vệ endpoint /api/cron/notify, tự sinh 1 chuỗi random
CRON_SECRET="change-me-to-a-random-secret"
